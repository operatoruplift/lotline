# Controlled browser verification screenshots

`pyth-320.png` comes from the Playwright market-reference test at a 320px viewport. All prices, confidence observations and feed IDs in this image are synthetic test fixtures. It verifies wrapping, controls and accessibility, not authenticated Pyth delivery. Actual current-price access remains unverified pending the server key. A temporary in-app estimate toast is visible in the capture.
