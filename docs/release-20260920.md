# Lotline production release — September 20, 2026

This is an authorized release of the existing application, following the [September 19 local checkpoint](finish-report-20260919.md). Local dates use Asia/Ho_Chi_Minh; evidence timestamps use UTC.

## Included changes

Exact receipt/recovery checks, immutable atomic execution journaling, unsigned-expiry recovery, duplicate-approval protection, downloadable receipts, stable manual reminder snapshots/cloud sync, and the explicitly labeled silent controlled walkthrough on `/demo`. Preserve the selected design, logo, 832-identity Example, ten-asset calculations, brand kit and both narrated planner films.

## Verified before deployment

- September 19 baseline: 336 deterministic tests and 80 distinct browser scenarios passed, plus a read-only mainnet check. No funded transaction was performed.
- September 20: targeted lint, production build and 8 focused production-build browser tests passed, including all three videos at 390/1440px, accessibility, install guidance, offline math/export and the disabled execution boundary.
- General and TypeScript code review approved the release.
- Supabase preflight found zero execution runs, legs, attempts, events, schedules and occurrences; original indexes/constraints matched expected history.
- Applied `20260915090000_guest_execution_owner_index.sql` then `20260919090000_execution_integrity.sql` through the authenticated linked CLI. All six local/remote versions now match.
- Hosted rolled-back assertions passed for atomic projections/events, one active attempt per run, immutable minimum output, unsigned expiry, signed expiry rejection and unresolved retention. All test rows were rolled back; this single-session check is not a multi-session load certification.
- Forced RLS and no anon/authenticated access remain on the private journal/quota tables; service-role event update/delete permissions are revoked. Supabase reports only eight informational no-policy notices for intentionally inaccessible tables: [advisor explanation](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy).

## Production configuration

Journal, guest and integrity migration readiness flags are true; repository is `supabase`. Execution and validator readiness flags are false. Existing Supabase/RPC credentials remain server-side. No credentials were invented, printed or added to source. Jupiter credentials and issuer/distributor access policy still need configuration; the actual encoded swap-instruction validator remains unfinished and cannot be bypassed by flags.

## Release and rollback boundary

Application deployment/hosted checks are recorded after the source release. Preserve all migrations and receipts on rollback. Disable new purchases and reconciliation writes before returning to older journal-writing code; do not drop the new owner indexes, delete unknown attempts or roll back tables. No funds, emails or competition submissions are authorized by this deployment.
