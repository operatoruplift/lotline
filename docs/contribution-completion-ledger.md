# Contribution completion ledger — September 20, 2026

Baseline: clean `c4e48e1`, following the verified production release in [release-20260920.md](release-20260920.md). The consolidated implementation prompt is the current requirement. Deployment is authorized; funding, financial signatures and event submission are not.

The table below records the **starting baseline only**. The completion checkpoint and linked current matrix below record the delivered state.

| Area | Baseline inspected state | Work/evidence boundary |
| --- | --- | --- |
| First-use Live planner | Defective | Search is hidden on first entry; empty drafts can be overwritten; failed catalog refresh loses display identities. Implement explicit illustrative starter and distinct loading/failure/unverified states without replacing saved input. |
| Exact contribution math, ten assets, sharing/export | Implemented | Preserve integer allocation, reviewed share consent and dated export values; existing unit/browser regressions. |
| xStocks identities/logos | Implemented | 832-identity dated snapshot and live issuer/mint checks; catalog size is not route availability. |
| Raw/scaled units | Defective metadata boundary | Official conversion exists but discards context; add observed slots/Clock/multiplier and explicit account/extension validation. |
| Quotes | Implemented, provider-dependent | Separate quote-only caller, no taker; verified keyless contract remains bounded. Execution authentication is a separate gate. |
| Semantic transaction validation | Missing | Envelope validation is insufficient. Implement a narrow encoded-instruction/account subset, lookups, chain/fee checks and unsigned simulation; unknown routes fail closed. |
| Wallet approval/submission | Implemented behind gate, real settlement unverified | Preserve exact-message signing and durable original signature before provider submission. No actual wallet or funds authorized. |
| Receipt/partial recovery | Implemented with fixture proof | Preserve unknown-state blocking and confirmed legs; extend account resolution/context for the semantic subset. |
| Execution journal/Supabase | Applied and verified | At baseline, six migrations matched; retain quotas, atomic attempts/events, owner/capability isolation and unresolved history. |
| Manual reminders | Implemented with fixture proof | Preserve local/cloud snapshots, stable occurrences, calendar export, timezone semantics and explicit review. Never automatic spending. |
| Accounts/email | Implemented; delivery externally blocked | Preserve sessions, cloud owner policies and historical real-account evidence. Public email stays gated until real sender delivery is proven. |
| L1/L4/L5/L6, mark/media | Implemented | Preserve exact manifest assets, green palette, visible motion, reduced motion and account controls. Capture actual before/after at 1440/768/390/320. |
| Demo/brand kit/PWA | Deployed and verified | Preserve two dated narrated films, controlled rehearsal, downloadable brand kit and public-only offline cache. Update current demonstrations after UI changes. |
| Sponsor lane | Decision requested | Keep current xStocks universe until an explicit choice; do not combine mutually exclusive pre-IPO lanes. |
| Optional Tokens.xyz | Externally blocked | No configured approved key; it must not block planning. |
| CI/license/release | Implemented | Existing MIT license and npm workflow; preserve dependencies and source/media attribution. New release needs fresh local/remote/hosted evidence. |

Each final result must separate source, deterministic tests, local browser, live reads, unsigned validation/simulation, real settlement, deployment and submission. No fixture or ready flag establishes settlement.

## Completion checkpoint

The baseline gaps above are preserved as inspection history. First-use, token context, semantic route validation, legacy-attempt guards and immutable ALT receipt recovery are implemented. The supported real unsigned order passed validation/simulation; all seven Supabase migrations are applied and behavior-checked. See the [current implementation/evidence matrix](contribution-release-20260920.md) for final source, test, hosted and external boundaries. Real settlement, public SMTP delivery, optional context and sponsor choice remain separate unresolved external items.
