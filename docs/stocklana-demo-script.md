# Lotline — 115-second Stocklana demo script

**Last updated:** September 12, 2026. This is an optional 115-second presentation/recording outline, not the transcript or duration of the [current product and technical films](video-release.md). Both current films have fresh Ainsley narration and redesigned application footage; original films remain archival sources. A production release has since been authorized; record the reviewed build identified in the [deployment record](deployment.md). Keep the address bar, mode labels and source times readable; no wallet connection, purchase, account creation or email is needed.

## Preparation

Use a separate browser profile so the recording does not replace a personal draft. Start with AAPLx, MSFTx and NVDAx at 50/30/20. Confirm a read-only Live quote route immediately before recording, but request a fresh estimate on camera; do not paste a historical price into the UI. Prepare the exact selected mint's evidence in **Verify this plan**. If Live is unavailable, use the alternate narration below and show the real error.

Use desktop capture with one brief mobile inset if available. The walkthrough uses three assets for clarity; the app supports ten. A saved device draft is sufficient for the repeat-use step. Optional named cloud saving requires an already authorized account and is not part of this guest demonstration.

Show the expanded Example with all 832 snapshot identities. The earlier public-site observation of six assets at September 12, 12:51 UTC is preserved in the [local finish report](stocklana-finish-delivery.md); it is not the status of a later release. The original six retain their practice values; additional assets use zero illustrative holdings and a generic one-unit-per-100-USDC rate. Keep the original three-asset walkthrough below so those generic rates do not distract from the contribution task. All Example values remain explicitly synthetic.

## Timed take

| Time | Screen/action | Narration |
| --- | --- | --- |
| 0:00–0:12 | Homepage, then **Try the example** | “You already know your xStocks and your split. Each new contribution still needs exact amounts and a plan for next time. This is Lotline: your next contribution, clearly.” |
| 0:12–0:30 | Keep **Example** visible. Show three chosen assets, percentages and budget; enter **10.000001** | “This is Example mode. Its balances and unit estimates are synthetic. My chosen split is fifty, thirty, twenty percent. These percentages apply to this contribution, not my existing portfolio.” |
| 0:30–0:45 | Show **5.000001 / 3.000000 / 2.000000**, open **Verify this plan**; copy/export | “Ten point zero zero zero zero zero one USDC becomes these exact amounts. The remaining micro-USDC goes to the largest remainder. Nothing disappears through rounding. I can inspect and export the calculation.” |
| 0:45–1:13 | Explicitly select **Live**. Load verified catalog, request estimates; inspect one successful asset's mint/program, quote source and actual time | “Now I’m switching to Live and requesting fresh, amount-specific estimates. The issuer identifies the asset; Solana verifies its mint. Token-2022 scaling can change displayed units, so Lotline converts raw amounts with the mint configuration and chain time. Jupiter supplies the route estimate. A plain percentage spreadsheet does not verify those live facts.” |
| 1:13–1:36 | Change budget to **25.000001**; point to results clearing and saved-device status. Reload the ordinary `/app` route; show same split and changed budget. Request new estimates only after reload | “The device draft keeps my chosen split. For the next contribution I change the amount, and the old estimates clear. Reloading restores my new budget and percentages. Fresh estimates are a separate request; saving a plan never freezes a price.” |
| 1:36–1:55 | Copy one exact Live allocation and verified mint. Choose **Open Jupiter** in the Review on Jupiter section; show only its landing/review step | “I copy the exact amount and mint, then open official Jupiter. I select that asset, enter the amount and review current fees and amounts there. Lotline does not sign, execute or confirm a trade. The plan is ready, and I can return to this split next time.” |

The new **25.000001** budget produces **12.500001 / 7.500000 / 5.000000 USDC** in the same 50/30/20 order. Do not copy an old quote after editing. Allow actual network latency; if needed trim silent waiting with a visible “request completed” transition while retaining original quote timestamps. Never make a later batch look like it refreshed an earlier result.

## Honest alternatives

- **Live unavailable:** stay visibly in Live long enough to show the actual error and say, “This provider is unavailable right now; Lotline leaves the estimate unavailable.” If including the prior verified read-only clip, label its date/origin and say, “This is earlier verification evidence, not a current quote.” Explicitly announce a return to Example before continuing with synthetic units. Do not call the current Live attempt successful.
- **Optional holdings:** only if time allows, use an independently public address suitable for display, state that it is a read-only lookup, and distinguish a confirmed zero from a failed lookup. A nonzero wallet is unnecessary to finish a contribution plan.
- **Named cloud plan:** a separate optional clip can show an existing account explicitly saving/loading a named plan. Do not imply that entering the app uploaded the device draft or that signup email has been verified.

Before sharing, play the export with captions, check every amount and mode transition, and verify the current source/live/video links. This script leaves no space for fabricated testimonials, successful-purchase screens or an unsupported placement claim.
